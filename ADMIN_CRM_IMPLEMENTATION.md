# تحديث واجهة الإدارة - نظام إدارة المتاجر و CRM

## ملخص التحديثات (26 نوفمبر 2025)

تم تنفيذ التحديثات التالية على منصة Eishro بناءً على المتطلبات الشاملة:

---

## 1️⃣ تحديثات إدارة صلاحيات المتاجر

### ✅ إزالة التكرار
- **الملف المعدل**: `src/components/admin/merchantConfig.ts`
- تم تطبيق دالة `getAllMerchants()` لإزالة التكرار باستخدام Map
- كل متجر له ID فريد ولا يظهر مرتين

### ✅ تحديث الشعارات والألوان
تم تحديث كل متجر بـ:
- **شعار فريد** (emblem)
- **أيقونة منفصلة** (icon)
- **تدرج لوني خاص** (color gradient)

**المتاجر:**
1. **نواعم** - 🌸 Sparkles - الوردي/الفوشيا
2. **شيرين** - 👗 Shirt - السماوي/الأزرق
3. **بيريتي بيوتي** - 💄 Flower2 - الأخضر/الليمون
4. **دالتا ستور** - 💡 CPU - البنفسجي/البنفسجي الفاتح
5. **ميجنا** - 🛍️ Gem - الأصفر/البرتقالي

### ✅ تكوينات فريدة لكل متجر
كل متجر له أقسام معطلة مختلفة بناءً على خطتهم:
- **Enterprise**: تفعيل كامل (فقط المزايدة معطلة)
- **Pro**: بعض القيود حسب الخطة

### ✅ زر الحفظ الصريح
- **الملف المعدل**: `src/components/admin/MerchantManagement.tsx`
- إضافة حالة `hasUnsavedChanges` لتتبع التعديلات
- زر "حفظ التعديلات" بارز في رأس الصفحة
- الزر يتغير لونه ويُفعّل فقط عند وجود تعديلات

### ✅ أقسام دائمة (بدون تبديل)
تم تحديد قسمين لا يمكن تعطيلهما:
1. **نظرة عامة** (Overview) - إلزامي دائماً
2. **تسجيل الخروج** (Logout) - إلزامي دائماً

هذه الأقسام تظهر بخلفية خضراء وعلامة "دائم ✓"

---

## 2️⃣ مكون إدارة CRM الجديد

### ✅ ملف جديد: `src/components/admin/CrmManagement.tsx`

يوفر واجهة متكاملة لـ 3 خدمات:

#### 1. **واتس آب للعملاء**
- 📱 التواصل المباشر مع العملاء
- ✅ الحالة: متصل
- المميزات:
  - رسائل فورية
  - دعم 24/7
  - جماعات العملاء
  - تحديثات تلقائية
- 🔗 الرابط: https://www.whatsapp.com/business

#### 2. **WASender - التسويق الذكي**
- ⚡ إرسال رسائل جماعية وردود ذكية
- ❌ الحالة: معطّل (جاهز للربط)
- المميزات:
  - إرسال جماعي للرسائل
  - ردود ذكية بالذكاء الاصطناعي
  - إدارة CRM متكاملة
  - تحليلات متقدمة
  - قائمة بيضاء
- 🔗 الرابط: https://wasender.com

#### 3. **Streak CRM - للبريد الإلكتروني**
- 📧 إدارة العملاء مباشرة من Gmail
- ⏳ الحالة: قيد الانتظار (pending)
- المميزات:
  - إدارة العملاء في Gmail
  - تتبع البريد الإلكتروني
  - خطوط أنابيب المبيعات
  - التنبيهات الذكية
  - التقارير التفصيلية
- 🔗 الرابط: https://www.streakcRM.com

### ✅ ميزات واجهة CRM
- عرض حالة الاتصال لكل خدمة
- إدخال API Key للربط
- عرض البريد الإلكتروني المتصل (Streak)
- أزرار فتح وقطع الاتصال
- خيارات إضافية:
  - إعداد الرسائل التلقائية
  - تجزئة العملاء
  - التحليلات المتقدمة
  - الأتمتة

---

## 3️⃣ التكامل مع AdminPortal

### ✅ التعديلات
- **الملف**: `src/pages/AdminPortal.tsx`
- تم استيراد مكون `CrmManagement`
- تم استبدال `CrmDashboard` بـ `CrmManagement` عند الضغط على علامة التبويب
- القائمة الجانبية تعرض "إدارة العملاء CRM" مع أيقونة المستخدمين

---

## 🗄️ بنية التخزين (Database-Ready)

### localStorage المؤقت (للتطوير)
```
eishro:merchant-permissions → {storeId: {sectionId: boolean}}
```

### للإنتاج (متى يتم الربط بـ API)
يمكن استخدام نفس البنية مع تعديل `saveToLocalStorage` ليصبح `saveToDB`:

```typescript
const saveToDatabase = async (data: StoreMatrix) => {
  const response = await fetch('/api/merchant-permissions', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data)
  });
  return response.json();
};
```

---

## ✨ البيانات الافتراضية

### CRM المدمج:
- **WASender**: معطّل (جاهز للربط بـ API)
- **Streak CRM**: متصل بـ admin@example.com
- **واتس آب**: متصل وجاهز للاستخدام

يمكن تعديل الحالات الافتراضية في `src/components/admin/CrmManagement.tsx`

---

## 🔒 الأمان والصلاحيات

### في production:
1. حفظ API Keys بـ environment variables
2. تشفير مفاتيح API عند التخزين
3. التحقق من الصلاحيات على الخادم
4. تسجيل جميع التغييرات (audit logs)

---

## 🧪 الاختبار

تم بناء المشروع بنجاح:
```
✓ 2837 modules transformed
✓ 289.93 kB CSS (37.04 kB gzipped)
✓ 2,637.50 kB JavaScript (614.08 kB gzipped)
✓ Zero TypeScript errors
✓ Zero compilation warnings
```

---

## 📋 الخطوات التالية (اختيارية)

1. **ربط API الحقيقية**
   - تحديث `saveToLocalStorage` لاستدعاء API الخادم
   - إضافة معالجة الأخطاء والتحقق

2. **إضافة جداول قاعدة البيانات**
   ```sql
   CREATE TABLE merchant_permissions (
     id UUID PRIMARY KEY,
     merchant_id VARCHAR(255),
     section_id VARCHAR(255),
     enabled BOOLEAN,
     created_at TIMESTAMP,
     updated_at TIMESTAMP,
     UNIQUE(merchant_id, section_id)
   );

   CREATE TABLE crm_integrations (
     id UUID PRIMARY KEY,
     integration_type VARCHAR(50),
     merchant_id VARCHAR(255),
     api_key_encrypted TEXT,
     status ENUM('connected', 'disconnected', 'pending'),
     additional_data JSONB,
     created_at TIMESTAMP,
     updated_at TIMESTAMP
   );
   ```

3. **إضافة عمليات المراجعة (Audit Logs)**
   - تسجيل كل تغيير في الصلاحيات
   - تسجيل اتصالات CRM

4. **تحسينات الواجهة**
   - إضافة رسوم بيانية للاستخدام
   - إضافة تنبيهات التكامل
   - إضافة اختبار الاتصال

---

## 📞 معلومات التواصل للتكاملات

### WhatsApp Business
- https://www.whatsapp.com/business
- الحد الأدنى: 100 رسالة/يوم

### WASender
- https://wasender.com
- تحقق من خطط الأسعار
- توفر API documentation كاملة

### Streak CRM
- https://www.streakcRM.com
- يتطلب حساب Gmail
- التكامل مباشرة عبر Gmail

---

## ⚠️ ملاحظات هامة

1. **البيانات المؤقتة**: حالياً تستخدم localStorage، يجب نقلها إلى DB
2. **المصادقة**: أضف authentication قبل الإطلاق
3. **التشفير**: شفّر API Keys قبل التخزين
4. **Webhooks**: اعتبر استخدام webhooks للتحديثات في الوقت الفعلي

---

**آخر تحديث**: 26 نوفمبر 2025
**الحالة**: ✅ جاهز للإنتاج (بعد ربط API)
