# قائمة التحقق الشاملة لـ Step 8 - إنشاء المتجر

## 🎯 مراحل التحقق

### المرحلة 1️⃣: قبل الرفع

```
تحديد المتجر:
- [ ] اسم المتجر باللغة العربية ✓
- [ ] اسم المتجر باللغة الإنجليزية ✓
- [ ] slug المتجر (بدون مسافات، مثل: delta-store) ✓
- [ ] الوصف الكامل للمتجر ✓
- [ ] الأيقونة (emoji أو صورة) ✓
- [ ] اللون الأساسي للمتجر ✓

المنتجات:
- [ ] عدد المنتجات محسوب بشكل صحيح ✓
- [ ] كل منتج له صورة على الأقل ✓
- [ ] الأسعار والأسعار الأصلية محددة ✓
- [ ] الفئات معرفة بشكل صحيح ✓

الشعار والصور:
- [ ] شعار المتجر موجود (.png أو .webp) ✓
- [ ] صور المنتجات بجودة عالية ✓
- [ ] صور الشريط السفلي موجودة ✓
- [ ] حجم الملفات لا يتجاوز 10MB لكل ملف ✓

البريد الإلكتروني:
- [ ] بريد المالك الأساسي صحيح ✓
- [ ] البريد الثاني (اختياري) موجود ✓
- [ ] البريد لم يُستخدم من قبل ✓
```

---

### المرحلة 2️⃣: أثناء الرفع

#### فحص السجلات في Backend Console:

```bash
# يجب أن ترى هذه الرسائل بالترتيب:
🔄 Starting store creation for: <متجر اسم>
📁 Moving X files from temp directory...
✅ Successfully moved X files to /assets/<slug>/
🔍 Checking if store already exists...
✅ Store uniqueness check passed
🔍 Checking email 1: email@example.com
✅ Email 1 is unique: email@example.com
📊 Parsing JSON data...
✅ Parsed: X products, Y sliders
🖼️ Mapping product images to products...
✅ Mapped images to products
🖼️ Mapping slider images...
✅ Mapped slider images
🏪 Logo URL: /assets/<slug>/logo/...
🚀 Generating store files...
📁 Created TS store directory: src/data/stores/<slug>
✅ TS files generated for development
📁 Created asset directory: public/assets/<slug>
✅ Created store.json at: public/assets/<slug>/store.json
✅ Updated stores index for: <slug>
✅ Verified: store.json
✅ Verified: index.json
✅ Verified: TS store directory
  ✅ config.ts
  ✅ products.ts
  ✅ Slider.tsx
  ✅ index.ts
  ✅ sliderData.ts
🎯 All verification checks passed for: <slug>
✅ JSON files generated for production
🎉 Store files generated successfully for: <متجر اسم>
🎉 Store successfully created with images: <متجر اسم>
```

#### ⚠️ إذا رأيت رسائل خطأ:

```
❌ Missing required fields: storeSlug, storeName, storeId
→ تأكد من ملء جميع الحقول المطلوبة

❌ Store with name "..." or slug "..." already exists
→ المتجر موجود بالفعل، استخدم اسم مختلف

❌ Email "..." is already registered
→ البريد مستخدم من قبل، استخدم بريد جديد

❌ Failed to move uploaded files
→ مشكلة في نقل الملفات، تحقق من صلاحيات الكتابة

❌ Store creation verification failed
→ أحد الملفات المطلوبة غير موجود، تحقق من السجلات
```

---

### المرحلة 3️⃣: بعد الرفع مباشرة

#### 1. التحقق من الملفات في النظام:

```bash
# تحقق من وجود الملفات
# في src/data/stores/<slug>/
  ✓ config.ts
  ✓ products.ts
  ✓ Slider.tsx
  ✓ index.ts
  ✓ sliderData.ts

# في public/assets/<slug>/
  ✓ store.json
  ✓ products/ (مجلد يحتوي صور المنتجات)
  ✓ sliders/ (مجلد يحتوي صور الشريط)
  ✓ logo/ (مجلد يحتوي الشعار)
```

#### 2. التحقق من محتوى `store.json`:

```bash
# افتح: public/assets/<slug>/store.json
# تحقق من أنه يحتوي على:
{
  "id": <رقم>,
  "slug": "<slug>",
  "name": "<الاسم>",
  "nameAr": "<الاسم العربي>",
  "nameEn": "<الاسم الإنجليزي>",
  "description": "<الوصف>",
  "logo": "/assets/<slug>/logo/...",
  "categories": ["فئة1", "فئة2"],
  "products": [ ... ],
  "sliderImages": [ ... ],
  "status": "active"
}
```

#### 3. التحقق من `index.json`:

```bash
# افتح: public/assets/stores/index.json
# تحقق من أن المتجر الجديد موجود:
[
  {
    "slug": "<slug>",
    "name": "<الاسم>",
    "nameAr": "<الاسم العربي>",
    "nameEn": "<الاسم الإنجليزي>",
    "description": "<الوصف>",
    "logo": "/assets/<slug>/logo/...",
    "categories": ["فئة1", "فئة2"],
    "productsCount": <عدد المنتجات>,
    "lastUpdated": "<التاريخ>"
  }
]
```

---

### المرحلة 4️⃣: الاختبار في المتصفح

#### التحقق من ظهور المتجر:

```
1. افتح الصفحة الرئيسية
2. ابحث عن قسم "متاجر على منصة إشرو"
3. يجب أن يظهر المتجر الجديد في الكاروسيل
4. [ ] المتجر يظهر
5. [ ] الشعار يظهر بشكل صحيح
6. [ ] الاسم مرئي
7. [ ] الوصف مرئي
8. [ ] الفئات تظهر
```

#### اختبار الروابط:

```
1. انقر على المتجر
2. يجب أن يذهب إلى: /<slug>
3. [ ] الانتقال نجح
4. [ ] الصفحة تحمل بدون أخطاء
5. [ ] المنتجات تظهر
6. [ ] الصور تحمل بشكل صحيح
7. [ ] الشريط العلوي يعمل
```

#### التحقق من الصور:

```
1. انقر كليك يمين على صورة المتجر → فتح في علامة تبويب جديدة
2. يجب أن يكون الرابط مثل: /assets/<slug>/logo/...
3. [ ] الرابط صحيح
4. [ ] الصورة تحمل بدون أخطاء
5. [ ] جودة الصورة جيدة

افعل نفس الشيء لصور المنتجات والشريط
```

---

## 📊 جدول الفحوصات

| العنصر | الموقع | الحالة | الملاحظات |
|--------|--------|--------|---------|
| config.ts | src/data/stores/<slug>/ | ✅ | يحتوي على معلومات المتجر |
| products.ts | src/data/stores/<slug>/ | ✅ | يحتوي على قائمة المنتجات |
| Slider.tsx | src/data/stores/<slug>/ | ✅ | مكون الشريط |
| index.ts | src/data/stores/<slug>/ | ✅ | يُصدّر جميع الملفات |
| sliderData.ts | src/data/stores/<slug>/ | ✅ | بيانات الشريط |
| store.json | public/assets/<slug>/ | ✅ | JSON كامل المتجر |
| index.json | public/assets/stores/ | ✅ | فهرس المتاجر |
| شعار المتجر | public/assets/<slug>/logo/ | ✅ | شعار بصيغة صحيحة |
| صور المنتجات | public/assets/<slug>/products/ | ✅ | جميع صور المنتجات |
| صور الشريط | public/assets/<slug>/sliders/ | ✅ | جميع صور الشريط |

---

## 🚀 أداة التحقق الآلية

```bash
cd backend

# شغل أداة التحقق:
npm run stores:verify

# المخرجات:
📚 Found 6 stores in src/data/stores/:

📦 Processing store: <slug>
─────────────────────────────────────
  Name: <الاسم>
  Icon: 🏪
  Logo: /assets/<slug>/logo/...
  Description: <الوصف>
  Products: X
  Categories: <الفئات>
  ✅ store.json exists

📊 VERIFICATION SUMMARY
==================================================
Total stores found: 6
Stores created: 1
Stores updated: 0
Stores skipped: 0

✅ Updated index.json with 6 stores
```

---

## ✅ الحالات الناجحة

### ✨ متجر تم إنشاؤه بنجاح:

```
الملفات الموجودة:
✅ src/data/stores/<slug>/config.ts
✅ src/data/stores/<slug>/products.ts
✅ src/data/stores/<slug>/Slider.tsx
✅ src/data/stores/<slug>/index.ts
✅ src/data/stores/<slug>/sliderData.ts
✅ public/assets/<slug>/store.json
✅ public/assets/<slug>/products/*.jpg
✅ public/assets/<slug>/sliders/*.jpg
✅ public/assets/<slug>/logo/logo.png
✅ public/assets/stores/index.json (يحتوي على <slug>)

في المتصفح:
✅ يظهر في الكاروسيل
✅ يمكن الضغط عليه
✅ الصور تحمل بدون أخطاء
✅ جميع الروابط تعمل

السجلات:
✅ جميع الرسائل خضراء
✅ لا توجد أخطاء
✅ جميع الفحوصات نجحت
```

---

## ❌ الحالات الفاشلة والحلول

### المشكلة 1: المتجر لا يظهر

**الأسباب المحتملة:**
- [ ] `index.json` لم يُحدّث
- [ ] slug المتجر غير موجود في الفهرس
- [ ] المتصفح كاش قديم

**الحل:**
```bash
# امسح كاش المتصفح (Ctrl+Shift+Delete)
# أو شغل:
npm run stores:verify

# ثم أعد تحميل الصفحة (Ctrl+F5)
```

### المشكلة 2: أخطاء 404 للصور

**الأسباب المحتملة:**
- [ ] الملفات لم تُنقل إلى الموقع الصحيح
- [ ] أسماء الملفات تختلف
- [ ] المسارات في JSON غير صحيحة

**الحل:**
```bash
# تحقق من وجود الملفات:
ls -la public/assets/<slug>/

# إذا كانت ناقصة:
npm run stores:verify
```

### المشكلة 3: خطأ في إنشاء الملفات

**الأسباب المحتملة:**
- [ ] مشكلة صلاحيات في القرص
- [ ] مجلد المشروع محمي من الكتابة
- [ ] مساحة تخزين ناقصة

**الحل:**
```bash
# تحقق من الصلاحيات:
chmod -R 755 public/assets/
chmod -R 755 src/data/stores/

# أو شغل الأداة مرة أخرى:
npm run stores:verify
```

---

## 📋 النموذج النهائي

بعد الانتهاء من جميع الفحوصات:

```
✅ معلومات المتجر:
   - الاسم: [      ]
   - Slug: [      ]
   - الوصف: [      ]

✅ الملفات المرفوعة:
   - الشعار: [      ] ✓
   - صور المنتجات: [X ملف] ✓
   - صور الشريط: [X ملف] ✓

✅ السجلات:
   - جميع الرسائل خضراء ✓
   - لا توجد أخطاء ✓

✅ قاعدة البيانات:
   - المتجر مضاف ✓
   - المالك مضاف ✓

✅ واجهة المستخدم:
   - يظهر في الكاروسيل ✓
   - الصور تحمل ✓
   - الروابط تعمل ✓

الحالة: 🎉 جاهز للإنتاج
```

---

**آخر تحديث:** 2025-11-21
**الإصدار:** 1.0
