# دليل اختبار النظام - إنشاء متجر بـ 10 خطوات

## 🚀 كيفية تشغيل التطبيق

### 1. تشغيل الخادم الأمامي (Frontend)
```bash
cd "C:\Users\dataf\Downloads\Eishro-Platform_V7"
npm run dev
```
سيتم فتح التطبيق على: `http://localhost:5173` (أو رقم منفذ مختلف)

### 2. تشغيل الخادم الخلفي (Backend) - في نافذة منفصلة
```bash
cd "C:\Users\dataf\Downloads\Eishro-Platform_V7\backend"
npm run dev
```
سيتم تشغيل الخادم على: `http://localhost:4000`

---

## ✅ خطوات الاختبار الشاملة

### الخطوة 1️⃣: الشروط والخصوصية
**ما يجب اختباره:**
- ✓ قراءة الشروط والخصوصية
- ✓ زر "السابق" معطل (لا يمكن الرجوع)
- ✓ زر "التالي" معطل طالما لم يتم الموافقة
- ✓ بعد الموافقة، زر "التالي" يعمل

**خطوات الاختبار:**
1. افتح صفحة إنشاء المتجر
2. اقرأ الشروط والخصوصية
3. حاول النقر "التالي" قبل الموافقة → يجب أن يظهر خطأ
4. اختر "أوافق على الشروط والخصوصية"
5. انقر "التالي" → يجب الذهاب للخطوة 2

### الخطوة 2️⃣: معلومات صاحب المتجر
**ما يجب اختباره:**
- ✓ إدخال الاسم الكامل
- ✓ إدخال بريد إلكتروني صحيح
- ✓ إدخال رقم هاتف
- ✓ التحقق من عدم ترك الحقول فارغة

**خطوات الاختبار:**
1. أدخل: "محمد أحمد"
2. أدخل: "test@example.com"
3. أدخل: "0912345678"
4. انقر "التالي" → يجب الذهاب للخطوة 3
5. حاول ترك حقل فارغ → يجب ظهور خطأ

**بيانات اختبار تجريبية:**
- الاسم: Salem Eshger
- البريد: salem.eshger@gmail.com
- الهاتف: 0912345678

### الخطوة 3️⃣: معلومات المتجر والوثائق
**ما يجب اختباره:**
- ✓ إدخال اسم المتجر بالعربية
- ✓ إدخال اسم المتجر بالإنجليزية
- ✓ إدخال وصف المتجر
- ✓ اختيار فئات المتجر
- ✓ رفع السجل التجاري
- ✓ رفع رخصة المزاولة
- ✓ إدخال subdomain وعرض الرابط النهائي

**خطوات الاختبار:**
1. أدخل اسم المتجر: "Indeesh"
2. أدخل اسم المتجر بالإنجليزية: "Indeesh Store"
3. أدخل وصف: "متجر متخصص في بيع الملابس والأزياء العصرية"
4. اختر فئة واحدة أو أكثر (مثلاً: الأزياء والملابس)
5. اختر ملف السجل التجاري (صورة أو PDF)
6. اختر ملف رخصة المزاولة (صورة أو PDF)
7. أدخل subdomain: "indeesh"
8. تحقق من عرض الرابط: "indeesh.eshro.ly"
9. انقر "التالي" → يجب الذهاب للخطوة 4

### الخطوة 4️⃣: إعدادات الحساب وشعار المتجر
**ما يجب اختباره:**
- ✓ إدخال كلمة مرور قوية (8+ أحرف)
- ✓ تأكيد كلمة المرور
- ✓ التحقق من تطابق كلمات المرور
- ✓ رفع شعار المتجر

**خطوات الاختبار:**
1. أدخل كلمة مرور: "SecurePass123"
2. أكد كلمة المرور: "SecurePass123"
3. انقر "اختر صورة الشعار" واختر ملف صورة
4. انقر "التالي" → يجب الذهاب للخطوة 5

### الخطوة 5️⃣: مراجعة البيانات
**ما يجب اختباره:**
- ✓ عرض جميع البيانات المدخلة
- ✓ إمكانية الرجوع والتعديل
- ✓ أزرار "تعديل" تعود للخطوة الصحيحة

**خطوات الاختبار:**
1. راجع جميع البيانات
2. انقر "تعديل" بجانب اسم المتجر → يجب الرجوع للخطوة 3
3. انقر "السابق" → يجب الرجوع للخطوة 4
4. انقر "التالي" → يجب الذهاب للخطوة 6

### الخطوة 6️⃣: إضافة المنتجات
**ما يجب اختباره:**
- ✓ إضافة منتج جديد
- ✓ إدخال معلومات المنتج (الاسم، الوصف، السعر، الحجم)
- ✓ رفع صور المنتج
- ✓ عداد المنتجات (0/30)
- ✓ تعديل وحذف المنتجات

**خطوات الاختبار:**
1. أدخل اسم المنتج: "فستان أسود"
2. أدخل الاسم الإنجليزي: "Black Dress"
3. أدخل الوصف: "فستان أنيق باللون الأسود"
4. أدخل السعر: "50"
5. اختر حجم: "L"
6. أضف لوناً: "أسود"
7. رفع صورة المنتج (1-3 صور على الأقل)
8. انقر "إضافة المنتج"
9. تحقق من ظهور المنتج في القائمة
10. انقر "التالي" → يجب الذهاب للخطوة 7

### الخطوة 7️⃣: إضافة صور السلايدرز
**ما يجب اختباره:**
- ✓ إضافة صورة سلايدر
- ✓ إدخال عنوان وصف الصورة
- ✓ إدخال نص الزر
- ✓ عداد صور السلايدر

**خطوات الاختبار:**
1. رفع صورة السلايدر
2. أدخل عنوان: "تشكيلة جديدة"
3. أدخل وصف فرعي: "اكتشفي أحدث الموديلات"
4. أدخل نص الزر: "تسوقي الآن"
5. انقر "إضافة الصورة"
6. تحقق من ظهور السلايدر
7. أضف 2-3 سلايدرز على الأقل
8. انقر "التالي" → يجب الذهاب للخطوة 8

### الخطوة 8️⃣: موقع المخزن
**ما يجب اختباره:**
- ✓ إدخال عنوان المخزن
- ✓ اختيار المدينة من القائمة
- ✓ إدخال رقم هاتف المخزن (اختياري)

**خطوات الاختبار:**
1. أدخل عنوان المخزن: "طرابلس، المدينة الجديدة، شارع النيل، مبنى رقم 10"
2. اختر مدينة: "طرابلس"
3. أدخل رقم الهاتف (اختياري): "0912345678"
4. انقر "التالي" → يجب الذهاب للخطوة 9

### الخطوة 9️⃣: التأكيد النهائي
**ما يجب اختباره:**
- ✓ عرض رسالة التأكيد
- ✓ وجود زر "إنشاء المتجر"

**خطوات الاختبار:**
1. اقرأ الرسالة التأكيدية
2. تحقق من جميع البيانات أخيراً
3. انقر "إنشاء المتجر"

### الخطوة 🔟: نجاح الإنشاء
**ما يجب اختباره:**
- ✓ عرض رسالة النجاح
- ✓ "تم إنشاء المتجر بنجاح! 🎉"
- ✓ إعادة التوجيه التلقائي

---

## 📊 نقاط فحص الأداء

### التحقق من الأخطاء:
```bash
npm run lint
# يجب أن ينجح دون أخطاء
```

### التحقق من صحة TypeScript:
```bash
npx tsc --noEmit
# يجب أن ينجح دون أخطاء
```

### البناء:
```bash
npm run build
# يجب أن ينجح
```

---

## 🔍 اختبار متقدم

### 1. اختبار الملاحة:
- ✓ زر "السابق" يعود للخطوة السابقة
- ✓ الضغط على "تعديل" يأخذك للخطوة الصحيحة
- ✓ شريط التقدم يعدّل نسبته

### 2. اختبار التحقق:
- ✓ عدم ترك حقول مطلوبة فارغة
- ✓ التحقق من صيغة البيانات
- ✓ عرض رسائل خطأ واضحة

### 3. اختبار الأداء:
- ✓ سرعة التنقل بين الخطوات
- ✓ عدم تجميد التطبيق عند رفع الصور الكبيرة
- ✓ التحقق من استقراري العمل مع عدد كبير من المنتجات

---

## 🎯 معايير النجاح النهائي

✅ جميع الخطوات 10 تعمل بدون أخطاء
✅ التحقق من البيانات يعمل لكل خطوة
✅ الملاحة سلسة بين الخطوات
✅ شريط التقدم يعدّل بصحة
✅ الرسائل واضحة وفي الوقت المناسب
✅ يمكن العودة والتعديل على أي خطوة
✅ المتجر ينشأ بنجاح في النهاية

---

## 📞 معلومات التواصل في حالة المشاكل

### مشكلة في الخطوة X:
1. تحقق من رسالة الخطأ
2. تأكد من إدخال جميع البيانات المطلوبة
3. حاول إعادة تحميل الصفحة
4. تحقق من وحدة التحكم (Console) للأخطاء

### مشكلة في البناء:
```bash
npm install
npm run build
```

### مشكلة في الخادم الخلفي:
```bash
cd backend
npm install
npm run dev
```

---

**نتمنى لك اختباراً ناجحاً! 🎉**
