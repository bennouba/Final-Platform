# قائمة التحقق من إنشاء المتجر - Store Creation Verification Checklist

**تاريخ الاختبار**: _______________  
**الحالة**: _______________

---

## ✅ قبل الإنشاء

### البيئة والإعدادات
- [ ] Backend يعمل بنجاح
- [ ] Frontend يعمل بنجاح
- [ ] public/assets موجود وقابل للكتابة
- [ ] src/data/stores موجود وقابل للكتابة
- [ ] قاعدة البيانات متصلة

### التحضيرات
- [ ] تنظيف .tmp-uploads القديم
  ```bash
  rm -rf .tmp-uploads
  mkdir -p .tmp-uploads
  ```
- [ ] التحقق من المساحة الحرة في القرص الصلب
- [ ] تشغيل البرنامج في بيئة تطوير

---

## ✅ أثناء الإنشاء

### الخطوة 1: تعبئة البيانات
- [ ] إدخال اسم المالك
- [ ] إدخال البريد الإلكتروني
- [ ] إدخال رقم الهاتف
- [ ] إدخال اسم المتجر (عربي)
- [ ] إدخال اسم المتجر (إنجليزي)
- [ ] إدخال وصف المتجر
- [ ] اختيار الفئات (على الأقل 1)
- [ ] إدخال subdomain فريد
- [ ] رفع الملفات الثبوتية
- [ ] إدخال كلمة المرور

### الخطوة 2: المنتجات
- [ ] إضافة على الأقل منتج واحد
- [ ] إدخال اسم المنتج
- [ ] إدخال الوصف
- [ ] تعيين السعر
- [ ] اختيار الألوان والأحجام
- [ ] رفع صور المنتج (على الأقل صورة واحدة)
- [ ] التحقق من الصور المعاينة

### الخطوة 3: صور السلايدر
- [ ] إضافة على الأقل صورة سلايدر واحدة
- [ ] إدخال العنوان
- [ ] إدخال الوصف الفرعي
- [ ] نص الزر
- [ ] رفع الصورة

### الخطوة 4: الشعار
- [ ] رفع شعار المتجر
- [ ] التحقق من معاينة الشعار

### الخطوة 5: مراجعة البيانات
- [ ] مراجعة جميع البيانات
- [ ] التحقق من الأخطاء

### الخطوة 6-8: الإرسال والتأكيد
- [ ] النقر على "إنشاء المتجر"
- [ ] انتظار اكتمال العملية
- [ ] ظهور رسالة النجاح

---

## ✅ بعد الإنشاء

### المرحلة 1: السجلات (Logs)
- [ ] فتح سجلات Backend
  ```bash
  tail -100 backend.log
  ```
- [ ] البحث عن "🚀 Starting file migration"
- [ ] التحقق من "✅ File migration completed successfully"
- [ ] البحث عن "🚀 Starting store file generation"
- [ ] التحقق من "🎉 Store generation COMPLETED"
- [ ] البحث عن "🔍 Verifying permanent storage"
- [ ] التحقق من "🎯 Store verification PASSED"

### المرحلة 2: الملفات المنقولة
```bash
# 1️⃣ فحص ملفات المنتجات
ls -la public/assets/{store-slug}/products/
# يجب أن تكون موجودة وبحجم > 0

# 2️⃣ فحص صور السلايدر
ls -la public/assets/{store-slug}/sliders/
# يجب أن تكون موجودة

# 3️⃣ فحص الشعار
ls -la public/assets/{store-slug}/logo/
# يجب أن يكون الشعار موجود
```

- [ ] منتج 1 موجود: `public/assets/{slug}/products/product_1_1.{ext}`
- [ ] منتج 2 موجود: `public/assets/{slug}/products/product_2_1.{ext}`
- [ ] صورة السلايدر موجودة: `public/assets/{slug}/sliders/slider_1.{ext}`
- [ ] الشعار موجود: `public/assets/{slug}/logo/store-logo.{ext}`

### المرحلة 3: ملفات TS (للتطوير)
```bash
# فحص ملفات TS
ls -la src/data/stores/{store-slug}/
```

- [ ] `config.ts` موجود
- [ ] `products.ts` موجود
- [ ] `Slider.tsx` موجود
- [ ] `index.ts` موجود
- [ ] `sliderData.ts` موجود

### المرحلة 4: ملفات JSON (الدائمة)
```bash
# 1️⃣ فحص index.json
cat public/assets/stores/index.json | jq '.[] | select(.slug=="{store-slug}")'

# 2️⃣ فحص store.json
cat public/assets/{store-slug}/store.json | jq '.'
```

- [ ] `public/assets/stores/index.json` محدث
- [ ] المتجر موجود في الفهرس
- [ ] `public/assets/{store-slug}/store.json` موجود
- [ ] بيانات store.json كاملة

### المرحلة 5: محتوى JSON
```bash
# تحقق من محتوى store.json
jq '.products | length' public/assets/{store-slug}/store.json
jq '.sliderImages | length' public/assets/{store-slug}/store.json
```

- [ ] عدد المنتجات صحيح: _____ (متوقع: _____)
- [ ] عدد صور السلايدر صحيح: _____ (متوقع: _____)
- [ ] مسارات الصور صحيحة في store.json
- [ ] الشعار محدث في store.json

### المرحلة 6: التنظيف
```bash
# تحقق من تنظيف الملفات المؤقتة
ls -la .tmp-uploads/ 2>/dev/null || echo "تم التنظيف بنجاح"
```

- [ ] `.tmp-uploads/` فارغ (تم التنظيف)
- [ ] لا توجد ملفات مؤقتة متبقية

---

## ✅ الواجهة الأمامية

### الاختبار 1: الصفحة الرئيسية
```javascript
// في console الصفحة الرئيسية
fetch('/assets/stores/index.json').then(r => r.json()).then(d => {
  const store = d.find(s => s.slug === '{store-slug}');
  console.log('Store in index:', store);
});
```

- [ ] المتجر يظهر في Carousel على الصفحة الرئيسية
- [ ] الشعار يظهر بشكل صحيح
- [ ] الاسم يظهر صحيح
- [ ] الوصف يظهر صحيح

### الاختبار 2: صفحة المتجر
```bash
# انقر على المتجر في Carousel
# انتظر تحميل الصفحة
```

- [ ] صفحة المتجر تحميل بنجاح
- [ ] الشعار يظهر على الصفحة
- [ ] المنتجات تظهر بشكل صحيح
- [ ] صور المنتجات تظهر (لا توجد 404)
- [ ] السلايدر يعمل بشكل صحيح
- [ ] صور السلايدر تظهر

### الاختبار 3: إعادة التحميل
```bash
# أعد تحميل الصفحة الرئيسية (F5)
# اضغط Ctrl+Shift+R (cache clear)
```

- [ ] المتجر لا يزال يظهر بعد التحميل
- [ ] البيانات من التخزين الدائم وليس من localStorage
- [ ] لا توجد أخطاء في console

---

## ✅ الفحوصات المتقدمة

### Curl Tests
```bash
# اختبر الفهرس
curl -s http://localhost:5173/assets/stores/index.json | jq '.[]'

# اختبر بيانات المتجر
curl -s http://localhost:5173/assets/{store-slug}/store.json | jq '.name'

# اختبر الصورة
curl -I http://localhost:5173/assets/{store-slug}/products/product_1_1.jpg
# يجب أن تكون 200 OK
```

- [ ] الفهرس يُسترجع بنجاح (200 OK)
- [ ] بيانات المتجر تُسترجع بنجاح (200 OK)
- [ ] الصور تُسترجع بنجاح (200 OK)

### Database Check
```bash
# تحقق من قاعدة البيانات
SELECT * FROM Stores WHERE slug = '{store-slug}';
```

- [ ] المتجر مسجل في قاعدة البيانات
- [ ] البيانات صحيحة

---

## ❌ معالجة المشاكل

### إذا لم يظهر المتجر

**الخطوة 1**: فحص السجلات
```bash
grep -i "error\|failed" backend.log
grep "store-slug" backend.log
```

**الخطوة 2**: فحص الملفات
```bash
test -f public/assets/stores/index.json && echo "✅ index.json exists" || echo "❌ index.json missing"
test -f public/assets/{store-slug}/store.json && echo "✅ store.json exists" || echo "❌ store.json missing"
```

**الخطوة 3**: إعادة الإنشاء
```bash
# احذف البيانات القديمة (احذر!)
rm -rf public/assets/{store-slug}
rm -rf src/data/stores/{store-slug}

# حاول مرة أخرى
```

### إذا كانت الصور 404

```bash
# فحص الملفات
ls -la public/assets/{store-slug}/products/
ls -la public/assets/{store-slug}/sliders/
ls -la public/assets/{store-slug}/logo/

# فحص المسارات في store.json
jq '.products[0].images' public/assets/{store-slug}/store.json
```

---

## 📊 النتيجة النهائية

### ✅ نجح الاختبار إذا:
- [ ] المتجر يظهر على الصفحة الرئيسية
- [ ] جميع الملفات موجودة في الأماكن الصحيحة
- [ ] الصور تظهر بدون أخطاء
- [ ] البيانات من التخزين الدائم
- [ ] إعادة التحميل تحافظ على المتجر
- [ ] السجلات تعرض رسائل نجاح

### ❌ فشل الاختبار إذا:
- [ ] المتجر لا يظهر
- [ ] هناك ملفات مفقودة
- [ ] الصور تعطي 404
- [ ] البيانات تختفي بعد التحميل
- [ ] توجد أخطاء في السجلات

---

**🎉 اختبار ناجح = نظام جاهز للإنتاج!**

---

## 📝 ملاحظات إضافية

الملاحظات:
_________________________________________________
_________________________________________________
_________________________________________________

المشاكل المواجهة:
_________________________________________________
_________________________________________________
_________________________________________________

التحسينات المقترحة:
_________________________________________________
_________________________________________________
_________________________________________________
