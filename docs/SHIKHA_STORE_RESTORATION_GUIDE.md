# دليل استعادة متجر شيخة بعد مسح LocalStorage

## 🚀 خطوات الاستعادة

### 1. **الوصول للأداة**
```
http://localhost:5174/restore-shikha-store.html
```

### 2. **فحص حالة المتجر**
- ستظهر لك حالة متجر شيخة
- إذا كان موجوداً، ستظهر رسالة "✅ تم العثور على متجر شيخة في النظام!"
- الأداة ستفحص أيضاً فهرس المتاجر وملف المتجر

### 3. **الوصول للتطبيق**
- انقر على زر "🚀 الذهاب للتطبيق"
- أو اذهب مباشرة إلى: `http://localhost:5174`

### 4. **البحث عن متجر شيخة**
- في الصفحة الرئيسية، ابحث عن "شيخة" في قائمة المتاجر
- انقر على المتجر لفتحه

## ✅ ما يجب أن تراه

### في الصفحة الرئيسية:
- متجر "شيخة" يجب أن يكون ظاهراً حتى بعد مسح localStorage
- الشعار: 🏪
- الوصف: ملابس نسائية راقية

### داخل المتجر:
- **3 منتجات**:
  1. بشت - 4560 د.ل (صورة: basht-1.jpeg)
  2. فستان - 4650 د.ل (صورة: fustan1.jpeg)  
  3. بشت - 4900 د.ل (صورة: basht-2.jpeg)

- **سلايدر بـ 2 صور**:
  - slider1-1.webp
  - slider2-1.webp

## 🔧 ما تم إصلاحه

### مشكلة LocalStorage:
- **قبل الإصلاح**: بعد مسح localStorage، متجر شيخة يختفي
- **بعد الإصلاح**: المتجر يظهر دائماً من الفهرس الدائم

### السبب والحل:
- كان متجر شيخة مفقود من "قائمة المتاجر المعروفة" في الكود
- تم إضافة 'shikha' إلى جميع القوائم المناسبة في `ModernStorePage.tsx`

## 🚨 استكشاف الأخطاء

### إذا لم يظهر متجر شيخة:

1. **تأكد من أن الباك إند يعمل**:
   - يجب أن يكون هناك خادم على `http://localhost:4000`
   - فحص console للباك إند

2. **تأكد من تحميل الفهرس**:
   ```javascript
   // في Developer Console
   fetch('http://localhost:4000/assets/stores/index.json')
     .then(response => response.json())
     .then(stores => console.log('Stores:', stores.filter(s => s.slug === 'shikha')));
   ```

3. **تأكد من تحميل بيانات المتجر**:
   ```javascript
   // في Developer Console
   fetch('http://localhost:4000/assets/shikha/store.json')
     .then(response => response.json())
     .then(store => console.log('Shikha store:', store));
   ```

### إذا ظهرت أخطاء:

1. **تحديث الصفحة**: اضغط F5 أو Ctrl+R
2. **مسح Cache**: 
   - Developer Console (F12)
   - Application → Storage → Clear Storage
   - أعد تحميل الصفحة

3. **إعادة تشغيل الخادم**:
   ```bash
   # في terminal
   npm run dev
   ```

## 📊 معلومات تقنية

### الملفات المهمة:
- `backend/public/assets/stores/index.json` - فهرس المتاجر
- `public/assets/shikha/store.json` - بيانات متجر شيخة
- `src/pages/ModernStorePage.tsx` - كود صفحة المتجر

### نقاط التحميل:
- فهرس المتاجر: `http://localhost:4000/assets/stores/index.json`
- بيانات المتجر: `http://localhost:4000/assets/shikha/store.json`
- الصور: `http://localhost:4000/assets/shikha/...`

## 🎯 النتيجة المتوقعة

بعد اتباع هذه الخطوات:
- ✅ متجر شيخة يجب أن يظهر في الصفحة الرئيسية
- ✅ المنتجات تظهر بصورها الحقيقية
- ✅ السلايدر يعمل بشكل صحيح
- ✅ المتجر يعمل حتى بعد مسح localStorage

---
**إذا واجهت أي مشاكل، تأكد من:**
1. تشغيل `npm run dev` بنجاح
2. المنفذ 5174 يعمل
3. المنفذ 4000 يعمل للباك إند
4. لا توجد أخطاء في Console