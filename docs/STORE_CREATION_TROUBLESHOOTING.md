# دليل حل مشاكل إنشاء المتجر - خطوة 8

## 🔴 المشكلة: عند النقر على زر "إنشاء المتجر" في الخطوة 8 لا يعمل

### 🔍 الأسباب المحتملة:

1. **الخادم (Backend) غير مشغل**
2. **البيانات غير مكتملة**
3. **الملفات (الصور والوثائق) لم تُرفع بشكل صحيح**
4. **الـ API Base URL غير صحيح**

---

## ✅ الحل خطوة بخطوة:

### 1️⃣ **تأكد من تشغيل الـ Backend**

```bash
# افتح Terminal جديد في المشروع
cd backend

# شغّل الخادم
npm run dev
```

**يجب أن تظهر رسالة:**
```
✅ Server is running on http://localhost:4000
🏥 Health check: http://localhost:4000/health
```

### 2️⃣ **تحقق من الاتصال بالـ Backend**

افتح الرابط في المتصفح:
```
http://localhost:4000/health
```

**يجب أن تظهر:**
```json
{
  "status": "ok",
  "environment": "development",
  "timestamp": "..."
}
```

إذا لم تظهر، الخادم غير مشغل. عد للخطوة 1️⃣

### 3️⃣ **تحقق من صحة البيانات قبل الضغط على الزر**

تأكد من ملء **جميع الحقول المطلوبة** في كل خطوة:

#### الخطوة 1: معلومات صاحب المتجر
- ✅ الاسم الكامل
- ✅ البريد الإلكتروني (صحيح)
- ✅ رقم الهاتف

#### الخطوة 2: معلومات المتجر
- ✅ اسم المتجر (عربي)
- ✅ اسم المتجر (إنجليزي)
- ✅ الوصف
- ✅ فئة واحدة على الأقل
- ✅ السجل التجاري (ملف)
- ✅ رخصة المزاولة (ملف)
- ✅ عنوان المتجر (subdomain - أحرف إنجليزية صغيرة فقط)

#### الخطوة 3: كلمة المرور
- ✅ كلمة المرور (8 أحرف على الأقل)
- ✅ تأكيد كلمة المرور (نفس الأولى)

#### الخطوة 4: شعار المتجر
- ✅ شعار المتجر (صورة)

#### الخطوة 5: مراجعة البيانات
- ✅ ليس هناك تحقق، فقط للمراجعة

#### الخطوة 6: المنتجات
- ✅ منتج واحد على الأقل مع:
  - الاسم
  - الوصف
  - السعر
  - الحجم
  - الصور

#### الخطوة 7: السلايدرز
- ✅ صورة واحدة على الأقل مع:
  - الصورة
  - العنوان
  - الوصف الفرعي
  - نص الزر

#### الخطوة 8: موقع المخزن
- ✅ عنوان المخزن (تفصيلي)
- ✅ المدينة (من القائمة)
- ✅ رقم الهاتف (اختياري)

### 4️⃣ **افتح وحدة التطوير (Developer Console) للرسائل التفصيلية**

```
اضغط: F12 أو Ctrl+Shift+I
اذهب إلى: Console
```

**اتبع الرسائل الآتية:**

1. `📤 Sending store creation request to: http://localhost:4000/api/stores/create-with-images`
2. `📨 Response status: 200`
3. `✅ Store created successfully`

إذا رأيت `❌ Store creation failed` أو `❌ Cannot reach backend`:
- تأكد من تشغيل الـ Backend (الخطوة 1️⃣)

### 5️⃣ **رسائل الأخطاء الشائعة وحلولها**

| الخطأ | السبب | الحل |
|--------|------|------|
| `الخادم غير متاح. تأكد من تشغيل الـ Backend` | Backend غير مشغل | شغّل `npm run dev` في مجلد backend |
| `البيانات غير مكتملة. الحقول المفقودة: ...` | حقول فارغة | عد واملأ الحقول المذكورة |
| `HTTP 500` | خطأ في الخادم | تحقق من سجلات Backend (Terminal) |
| `CORS error` | خطأ في الوصول | تأكد من CORS في `backend/src/app.ts` |

### 6️⃣ **إذا استمرت المشكلة**

**انسخ الرسالة من Console واتبع هذه الخطوات:**

1. افتح ملف `.env` في مجلد backend:
```
PORT=4000
NODE_ENV=development
```

2. تأكد من أن قيم المتغيرات صحيحة

3. أعد تشغيل Backend:
```bash
cd backend
npm run dev
```

4. امسح بيانات المتصفح:
```
اضغط: F12
اذهب إلى: Application → Local Storage
احذف جميع البيانات المتعلقة بـ localhost
ثم حدّث الصفحة
```

### 7️⃣ **رسائل النجاح المتوقعة**

عند الضغط على "إنشاء المتجر" بنجاح:

✅ **في Console:**
```
📤 Sending store creation request to: http://localhost:4000/api/stores/create-with-images
📨 Response status: 200
✅ Store created successfully via API
```

✅ **على الصفحة:**
نافذة بالرسالة:
```
تمت عملية الانشاء كاملة بنجاح !
نتمنى لك وقت ممتعا معنا
```

✅ **الملفات المُنشأة:**
```
public/assets/[your-subdomain]/
├── logo/
├── products/
└── sliders/

public/assets/stores/index.json
public/assets/[your-subdomain]/store.json
```

---

## 🆘 للحصول على مساعدة إضافية

**افتح Console وشارك الرسائل التالية:**
1. جميع الرسائل الحمراء (❌)
2. الرسائل الأخيرة قبل الخطأ
3. رابط الـ API المحاول الاتصال به

---

## 📝 ملاحظات مهمة

- ✅ الـ Backend يجب أن يكون مشغلاً **قبل** فتح الموقع
- ✅ جميع الملفات (.jpg, .png, etc) يجب أن تكون أقل من 10MB
- ✅ عنوان المتجر (subdomain) يجب أن يكون فريداً (لم يُنشأ من قبل)
- ✅ البريد الإلكتروني ورقم الهاتف يجب أن يكونا فريديين أيضاً
